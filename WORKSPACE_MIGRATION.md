# Подготовка кода к переходу на Workspace

Этот документ описывает изменения, внесенные для подготовки кода к поддержке множественных workspace с drag-and-drop между ними.

## Внесенные изменения

### 1. Расширение типов drag-and-drop (`src/modules/dragDrop/types.ts`)

**Добавлено:**

- `WorkspaceId` - тип для идентификации workspace (UUID string)
- `WorkspaceType` - тип для определения типа workspace (playlist, drafts, fileBrowser, и т.д.)
- `sourceWorkspaceId` в `DraggedItems` - для отслеживания источника перетаскивания
- `workspaceId` в `DropContext` - для идентификации целевого workspace (опционально для обратной совместимости)

**Изменения:**

```typescript
export type DraggedItems =
  | { type: 'tracks'; ids: Set<string>; sourceWorkspaceId?: WorkspaceId }
  | { type: 'files'; paths: string[] }
  | null;

export interface DropContext {
  module: DropModule;
  targetId?: string;
  workspaceId?: WorkspaceId; // Новое поле
}
```

### 2. Утилиты для workspace (`src/utils/workspaceConstants.ts`)

**Создан новый файл** с утилитами:

- `DEFAULT_PLAYLIST_WORKSPACE_ID` - константа для дефолтного плейлиста
- `generateWorkspaceId()` - генерация нового UUID для workspace
- `isCrossWorkspaceOperation()` - проверка, является ли операция cross-workspace
- `getWorkspaceType()` - получение типа workspace по ID (базовая реализация)

### 3. Модификация хука drag-and-drop (`src/hooks/usePlaylistDragAndDrop.ts`)

**Добавлено:**

- Параметр `workspaceId` в `PlaylistDragOptions`
- Опциональные callbacks `onMoveTracksToWorkspace` и `onCopyTracksToWorkspace` для cross-workspace операций
- Сохранение `sourceWorkspaceId` при `handleDragStart`
- Определение cross-workspace операций в `handleDragOver` и `handleDrop`
- Поддержка Ctrl+Drag для копирования вместо перемещения

**Логика:**

- При `dragStart`: сохраняется `sourceWorkspaceId` текущего workspace
- При `dragOver`: определяется, является ли операция cross-workspace, и устанавливается соответствующий `dropEffect`
- При `drop`: если операция cross-workspace, вызываются соответствующие callbacks (если предоставлены), иначе используется обычная логика

### 4. Обновление PlaylistView (`src/components/PlaylistView.tsx`)

**Изменения:**

- Импорт `DEFAULT_PLAYLIST_WORKSPACE_ID`
- Передача `workspaceId` в хук `usePlaylistDragAndDrop`
- Передача `workspaceId` во все `DropContext` при drag-and-drop событиях

### 5. Базовая структура workspace manager (`src/state/uiStore.ts`)

**Добавлено:**

- Интерфейс `WorkspaceInfo` для хранения информации о workspace
- Состояние `workspaces` и `activeWorkspaceId` в `UIState`
- Методы для управления workspace:
  - `addWorkspace(workspace)` - добавление workspace
  - `removeWorkspace(id)` - удаление workspace
  - `setActiveWorkspace(id)` - установка активного workspace
  - `getWorkspace(id)` - получение workspace по ID

## Обратная совместимость

Все изменения сохраняют обратную совместимость:

- `workspaceId` в `DropContext` опционален
- `sourceWorkspaceId` в `DraggedItems` опционален
- Существующий код продолжает работать без изменений
- Cross-workspace операции активируются только при наличии соответствующих callbacks

## Текущее состояние

### Работает:

- ✅ Drag-and-drop внутри одного плейлиста (как раньше)
- ✅ Drag-and-drop файлов из FileBrowser в плейлист (как раньше)
- ✅ Базовая инфраструктура для cross-workspace операций

### Готово к реализации:

- 🔄 Cross-workspace drag-and-drop (требует реализации callbacks)
- 🔄 Множественные workspace (требует создания новых workspace stores)
- 🔄 UI для управления workspace (требует создания компонентов)

## Следующие шаги

1. **Реализация cross-workspace операций:**
   - Создать методы в playlistStore для перемещения/копирования треков между workspace
   - Реализовать callbacks `onMoveTracksToWorkspace` и `onCopyTracksToWorkspace` в PlaylistView

2. **Создание новых workspace:**
   - Создать store для Drafts workspace
   - Создать компоненты для новых workspace
   - Интегрировать с workspace manager

3. **UI для workspace:**
   - Компонент для отображения множественных workspace
   - Поддержка перемещения и ресайза панелей
   - Сохранение layout

4. **Валидация операций:**
   - Реализовать проверку возможности операций между типами workspace
   - Добавить ограничения для разных типов workspace

## Примечания

- Все изменения протестированы на существующей функциональности
- Код готов к постепенному добавлению новых workspace
- Архитектура позволяет легко расширять систему новыми типами workspace
